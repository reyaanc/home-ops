---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.envoyproxy.io/securitypolicy_v1alpha1.json
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: copyparty-oidc
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: copyparty
  oidc:
    provider:
      issuer: https://id.reyaan.dev
    clientID: e52c8154-4ba7-46a5-96c6-180954012404
    clientSecret:
      name: copyparty-oidc-secret
    scopes: ["openid", "email", "profile", "groups", "offline_access"]
    redirectURL: https://copyparty.reyaan.dev/oauth2/callback
    logoutPath: /oauth2/sign_out
    cookieDomain: reyaan.dev
    refreshToken: true
    disableTokenEncryption: true
    cookieNames:
      idToken: copyparty-token
  jwt:
    providers:
      - name: pocketid
        issuer: https://id.reyaan.dev
        remoteJWKS:
          uri: https://id.reyaan.dev/.well-known/jwks.json
        extractFrom:
          cookies:
            - copyparty-token
        claimToHeaders:
          - claim: preferred_username
            header: x-idp-user
          - claim: groups
            header: x-idp-group
