---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: frigate-camera
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: frigate-camera-secret
    template:
      data:
        # Frigate only substitutes FRIGATE_-prefixed env vars in config.yaml,
        # so expose them prefixed (sourced from the plain 1Password fields).
        FRIGATE_CAMERA_USERNAME: "{{ .CAMERA_USERNAME }}"
        FRIGATE_CAMERA_PASSWORD: "{{ .CAMERA_PASSWORD }}"
  dataFrom:
    - extract:
        key: frigate
